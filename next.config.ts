import type { NextConfig } from "next";

const nextConfig = {
  images: {
      domains: ['deisishop.pythonanywhere.com', 'upload.wikimedia.org'], // Adiciona os domínios necessários
  },
};

export default nextConfig;




